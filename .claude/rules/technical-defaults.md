---
description: Technical stack defaults for HTML design recreation projects
alwaysApply: true
---

- Use Tailwind CSS via CDN (`<script src="https://cdn.tailwindcss.com"></script>`)
- Use placeholder images from `https://placehold.co/` when source images aren't provided
- Mobile-first responsive design
- Single `index.html` file unless the user requests otherwise
- Screenshot tool: Chrome headless at `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`